# Local editor and Python caches
.idea/
__pycache__/
*.py[cod]

# Ephemeral distributed-training state
torchelastic_*/

# Large and intermediate experiment artifacts are kept out of Git history.
# Curated final reports/figures are force-added. Model weights are intentionally
# excluded from the current GitHub snapshot. See ARTIFACTS.md.
comparison_outputs/
comparison_smoke/
results_*/

# Local release packaging workspace
.release-staging/
